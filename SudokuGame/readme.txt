Was use the backtracking algorithm.
Generate every possible option for each square until eventually there is a solution
Each square has nine options - each grid has 9 possibilities
As soon we find the required number that fits into the square, we move on to another and further

To run the game simply run the GUI.py file from IDLE or the command line.